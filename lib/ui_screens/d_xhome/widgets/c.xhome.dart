part of '_index.dart';

class XhomeC extends ConsumerWidget {
  const XhomeC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
