part of '_index.dart';

class XhomeD extends ConsumerWidget {
  const XhomeD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
