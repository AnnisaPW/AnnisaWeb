part of '_index.dart';

class XhomeE extends ConsumerWidget {
  const XhomeE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
