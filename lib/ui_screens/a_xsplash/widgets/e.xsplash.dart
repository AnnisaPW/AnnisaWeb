part of '_index.dart';

class XsplashE extends ConsumerWidget {
  const XsplashE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
