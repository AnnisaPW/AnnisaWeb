part of '_index.dart';

class XsplashD extends ConsumerWidget {
  const XsplashD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
