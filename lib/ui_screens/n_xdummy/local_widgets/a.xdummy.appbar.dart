part of '_index.dart';

class XdummyAppbar extends ConsumerWidget {
  const XdummyAppbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return AppBar(
      title: const Text('Xdummy'),
    );
  }
}
