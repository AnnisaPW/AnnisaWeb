part of '_index.dart';

class XprofileD extends ConsumerWidget {
  const XprofileD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
