part of '_index.dart';

class XprofileA extends ConsumerWidget {
  const XprofileA({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
