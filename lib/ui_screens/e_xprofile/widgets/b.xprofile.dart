part of '_index.dart';

class XprofileB extends ConsumerWidget {
  const XprofileB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
