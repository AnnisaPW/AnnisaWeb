part of '_index.dart';

class XprofileE extends ConsumerWidget {
  const XprofileE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
