part of '_index.dart';

class XprofileC extends ConsumerWidget {
  const XprofileC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
